<%= render "layouts/navbar", title: "Home" %>

<div id="categories">
  <% @categories.each do |category| %>
    <%= render category %>
  <% end %>
</div>
<div class="buttom">
  <%= link_to "add category", new_category_path, class: "btn btn-blu col-12"%>
</div>
